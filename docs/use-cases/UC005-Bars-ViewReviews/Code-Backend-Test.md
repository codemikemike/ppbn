# UC005 — Code (Backend Tests)

- Add repository unit tests for `barRepository.findBySlug`:
  - Returns a bar with mapped reviews
  - Returns null for unknown slug
- Mock `@/lib/db` to avoid real DB access.
