# UC010 — Summary

UC010 adds a dedicated SEO-focused "Tonight in Phnom Penh" page that aggregates:

- Events happening today
- Bars open tonight (based on opening hours)
- Live music venues tonight
- Featured bars

It is implemented with a single combined backend operation (`getTonightData`) and exposed via both the page and `GET /api/tonight`.
