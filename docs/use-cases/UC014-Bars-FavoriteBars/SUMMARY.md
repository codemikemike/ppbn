# UC014 — Summary

UC014 adds the ability for authenticated users to favorite/unfavorite bars from the bar detail page and view/remove favorites from a dedicated dashboard page.

## Deliverables

- Favorite toggle API: `POST /api/bars/[slug]/favorite`
- Heart icon client component with optimistic UI
- Dashboard favorites list page
- Repository + service methods respecting Clean Architecture
