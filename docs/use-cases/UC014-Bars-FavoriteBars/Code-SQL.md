# UC014 — Code (SQL / Schema)

## Prisma

This use case relies on the existing `FavoriteBar` table:

- Table: `favorite_bars`
- Unique: `(userId, barId)`

## Migrations

No new migration is required if `favorite_bars` already exists in the database.
