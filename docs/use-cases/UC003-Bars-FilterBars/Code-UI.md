# UC003 — Code-UI

## Bars Page

- Update `src/app/bars/page.tsx`.
- Add filter UI for:
  - Area: All, Riverside, BKK1, Street136, Street104
  - Category: All + all BarCategory values
- Filters are links that update URL search params.
- Active filter is highlighted.

## Semantic HTML

- Use `<nav>` with `aria-label` for filters.
