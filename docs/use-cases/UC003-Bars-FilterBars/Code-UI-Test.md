# UC003 — Code-UI-Test

- Clicking an Area filter updates URL with `?area=...`.
- Clicking a Category filter updates URL with `?category=...`.
- Active filter styles apply based on URL.
- Empty state renders when filtered results are empty.
