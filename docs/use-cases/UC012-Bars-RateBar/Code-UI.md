# UC012 — Code-UI

## Bar Detail Page

- Update `/bars/[slug]` to:
  - Show the interactive rating UI when the user is logged in.
  - Show "Login to rate this bar" when the user is not logged in.
  - Preload and display the user's existing rating.

## Components

- Add `StarRating` client component:
  - Hover states
  - Click-to-rate
  - Success/error feedback
