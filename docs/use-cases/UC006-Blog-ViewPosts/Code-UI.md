# UC006 — Code (UI)

- Add `/blog` list page.
- Render each post in an `<article>` with a proper heading structure.
- Show: title, excerpt, author, date, category.
- Provide pagination via `page` query param.
