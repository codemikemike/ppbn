# UC001 — Code-UI

## Route

- Add `/bars` page in `src/app/bars/page.tsx`.

## Behavior

- Requests bars and renders a list.
- Displays empty state when no bars exist.
- Displays error state on failure.

## UI Data

- Consumes `BarDto[]` only.
