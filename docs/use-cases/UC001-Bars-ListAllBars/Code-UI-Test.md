# UC001 — Code-UI-Test

## Goal

Verify the public bars list can be rendered and handles empty/error states.

## Approach

- Component test for the list rendering with `BarDto[]` props (if extracted to a component).
- E2E test (Playwright) for `/bars` page rendering.

## Note

UI testing tooling is not currently configured in this repository.
