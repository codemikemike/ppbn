# UC004 — Code-UI

## Bars Page

- Update `src/app/bars/page.tsx`.
- Add a search input above filter controls.
- Search is URL-driven via `?search=`.
- Clear button appears when search is active.
- Search combines with existing area/category filters.

## Semantic HTML

- Use a `<search>` element or `<form role="search">`.
