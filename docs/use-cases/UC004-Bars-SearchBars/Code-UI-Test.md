# UC004 — Code-UI-Test

- Search input shows the current `search` value from the URL.
- Submitting search updates `?search=`.
- Clear search button removes `search` param.
- Search combines with area/category params.
