# UC011 — Summary

UC011 adds a public interactive bar map using Leaflet.

- Bars with coordinates are shown as markers with popups.
- Filters by area and category are URL-driven.
- Coordinates are added to the Bar model and exposed via `BarDto`.
