# UC011 — Domain Model (DM)

```mermaid
classDiagram
  class Bar {
    +String id
    +String slug
    +String name
    +BarArea area
    +BarCategory category
    +Boolean isApproved
    +Float latitude
    +Float longitude
  }
```
