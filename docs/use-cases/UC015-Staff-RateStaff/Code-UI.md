# UC015 — Code (UI)

- `StaffStarRating` is a client component rendering 1–5 interactive stars.
- When logged out it displays "Login to rate".
- When logged in it submits ratings to `POST /api/staff/[id]/rate`.
