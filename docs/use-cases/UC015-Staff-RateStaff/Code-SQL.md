# UC015 — Code (SQL / Schema)

## Prisma Tables

This use case relies on existing tables:

- `staff_profiles`
- `staff_ratings` (unique `(staffProfileId, userId)`)

No new migration is required if these tables already exist.
