# UC015 — Summary

UC015 allows authenticated users to rate staff profiles (1–5 stars). The rating is stored as an upsert into `staff_ratings`, and the API returns both the user's rating and the updated average rating.
