# UC020 — Code-Backend

## API

- Add `POST src/app/api/auth/logout/route.ts`.
- Route clears NextAuth cookies and returns `{ ok: true }`.

## Notes

- No repositories/services are required for logout.
- Must not import `db`.
