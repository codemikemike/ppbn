# UC007 — Code (UI)

- Add `/blog/[slug]` page.
- Use semantic HTML:
  - `<main>` page wrapper
  - `<article>` for post
  - `<header>` for title/meta
  - `<aside>` for related posts
- Render featured image via `ImageWithFallback`.
- Add `generateMetadata`.
